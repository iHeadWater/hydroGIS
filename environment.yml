name: hydroGIS
channels:
  - conda-forge
  - defaults
dependencies:
  - python

  # Spatial packages
  - geopandas  
  - geopy
  - geojson
  - mapclassify>=2.2.0
  - contextily
  - osmnx
  - rasterstats
  - xarray-spatial
  - geemap
  - pykrige
  - pykriging
 

  # Jupyter Environment
  - jupyterlab
